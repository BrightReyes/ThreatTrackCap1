{
  "name": "ThreatTrackCap1",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix web-admin run dev",
    "build": "npm --prefix web-admin run build",
    "preview": "npm --prefix web-admin run preview",
    "admin:dev": "npm --prefix web-admin run dev",
    "admin:build": "npm --prefix web-admin run build",
    "landing:dev": "npm --prefix landing-page run dev",
    "landing:build": "npm --prefix landing-page run build"
  },
  "dependencies": {}
}